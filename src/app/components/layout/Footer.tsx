// ============================================================
// EnduroLab — Footer Component
// ============================================================

export default function Footer() {
  return (
    <footer className="border-t border-[var(--color-border)] bg-[var(--color-bg)] py-8">
      <div className="container-narrow text-center text-sm" style={{ color: "var(--color-text-secondary)" }}>
        <p>EnduroLab — Science-backed marathon training plans</p>
        <p className="mt-1">Built around Daniels&apos; Running Formula methodology</p>
      </div>
    </footer>
  );
}
